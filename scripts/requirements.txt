# ============================================================================
# INSTALLATION ORDER:
# 1. Create env: conda create -n tf5090 python=3.10 -y
# 2. Activate: conda activate tf5090
# 3. Install TF FIRST: pip install tensorflow[and-cuda]
# 4. Verify GPU: python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
# 5. Then install this file: pip install -r requirements.txt
# ============================================================================

# ============================================================================
# Core Scientific Computing & ML
# ============================================================================
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
h5py==3.12.1
joblib==1.4.2

# ============================================================================
# Image Processing & Computer Vision
# ============================================================================
opencv-python==4.10.0.84
Pillow==11.0.0

# ============================================================================
# Visualization & Plotting
# ============================================================================
matplotlib==3.9.2
seaborn==0.13.2

# ============================================================================
# Progress Bars & Terminal UI
# ============================================================================
tqdm>=4.62.0
rich==13.9.4

# ============================================================================
# Web/Network Utilities
# ============================================================================
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30

# ============================================================================
# Data Serialization & Config
# ============================================================================
pyyaml==6.0.2
packaging>=20.0

# ============================================================================
# Jupyter Environment (comment out if not needed)
# ============================================================================
jupyter==1.1.1
jupyterlab==4.2.6
notebook==7.2.2
ipykernel==6.29.5
ipython==8.29.0
ipywidgets==8.1.5

# ============================================================================
# IMPORTANT: TensorFlow + CUDA Installation
# ============================================================================
# These are NOT in this file because you must install tensorflow FIRST
# before other packages, and the version must be compatible with RTX 5090.
#
# What gets installed automatically with "pip install tensorflow[and-cuda]":
#   - tensorflow (latest version with compute capability 12.0 support)
#   - keras (bundled with TensorFlow 2.16+)
#   - tensorboard + tensorboard-data-server
#   - tensorflow-io-gcs-filesystem
#   - All CUDA/cuDNN libraries:
#       * nvidia-cublas-cu12
#       * nvidia-cuda-cupti-cu12
#       * nvidia-cuda-nvcc-cu12
#       * nvidia-cuda-nvrtc-cu12
#       * nvidia-cuda-runtime-cu12
#       * nvidia-cudnn-cu12
#       * nvidia-cufft-cu12
#       * nvidia-curand-cu12
#       * nvidia-cusolver-cu12
#       * nvidia-cusparse-cu12
#       * nvidia-nccl-cu12
#       * nvidia-nvjitlink-cu12
#   - All TensorFlow dependencies:
#       * absl-py, astunparse, flatbuffers, gast, google-pasta
#       * grpcio, libclang, markdown, opt-einsum, optree
#       * protobuf, termcolor, wrapt, typing-extensions
#       * ml-dtypes, namex
#
# The [and-cuda] extra ensures you get the CORRECT versions for your GPU.
# ============================================================================
